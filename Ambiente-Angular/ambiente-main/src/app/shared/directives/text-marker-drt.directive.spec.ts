import { TextMarkerDrtDirective } from './text-marker-drt.directive';

describe('TextMarkerDrtDirective', () => {
  it('should create an instance', () => {
    const directive = new TextMarkerDrtDirective();
    expect(directive).toBeTruthy();
  });
});
